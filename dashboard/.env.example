# API server port
PORT=8787

# Frontend API base URL (leave empty for same-origin)
VITE_API_URL=

# CORS allowed origins (comma-separated, for production)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8787

# Node environment
NODE_ENV=development
